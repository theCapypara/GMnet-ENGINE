/// ds_list_destroy_debug();
var check=11;

ds_list_destroy(argument0);

if check=argument0
{
    show_debug_message("============================List destroyed: " + string(check));
    var test=1;
}
